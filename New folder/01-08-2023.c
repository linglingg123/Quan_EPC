#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
//	char s[200];
//	 int count = 0;
//	 int i;
//	 printf("enter the string\n");
//	scanf("%s",&s);
//	 for (i = 0;i<strlen(s);i++)
//	 {
//	  if (s[i] == ' '){
//	  	count++;
//	  }
//	 }
//	 printf("number of words in given string are: %d\n", count);

isTri(2,3,40);

	//nuoc hoa
	struct nuocHoa{ //structure
		char hang[20]; //element
		char ten[20];
		float price;
	};
	
	nuocHoa.price;
	scanf("%d",nuocHoa.price);
	struct nuocHoa emp1 = {"Chanel","Bleu",17};
	struct nuocHoa emp2 = {"Chanel","Bleu",17};
	struct nuocHoa emp3 = {"Chanel","Bleu",17};
	struct nuocHoa emp4 = {"Chanel","Bleu",17};

	return 0;
}
struct tRI{ //structure
		char a[20]; //element
		char b[20];
		float c;
	};

int isTri(tRI){
	tRI.a*tRI.a
}
